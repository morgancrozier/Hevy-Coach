#!/bin/sh
# Documentation helper only: no configuration writes, network, or scheduler edits.
printf '%s\n' 'See AUTOMATION.md for opt-in automation and duplicate protection.'
printf '%s\n' 'Start offline with: python hevy_stats.py --demo'
printf '%s\n' 'This helper does not configure credentials, contact services, or install a schedule.'
